---
- hosts: all
  become: true
  tasks:
    - name: Echo and Show whoami
      debug:
        msg: "{{ lookup('pipe', 'whoami') }}"
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes
    - name: update apt packages
      apt:
        upgrade: yes
        update_cache: yes

    
